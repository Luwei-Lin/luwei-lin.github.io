/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '',
  trailingSlash: true,
  transpilePackages: ['@react-pdf/renderer'],
}

export default nextConfig
